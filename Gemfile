# Gemfile
source 'https://rubygems.org'

gem 'sinatra'  
gem 'mongoid'
# Required to use some advanced features of Sinatra, using namespaces
gem 'sinatra-contrib'

group :test do
  gem 'minitest'
  gem 'rack-test'
end
